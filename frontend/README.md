# Frontend

React + Vite + Tailwind CSS. Interface em português.

Run this app with Vite once dependencies are installed:

```bash
npm install
npm run dev
```

The dev server proxies `/api` to `http://localhost:8000`.

Theme: dark by default, toggle in the header, choice persisted in `localStorage` (`theme` key).
